import { CategoryIdByTypePipe } from './category-id-by-type.pipe';

describe('CategoryIdByTypePipePipe', () => {
  it('should be defined', () => {
    expect(new CategoryIdByTypePipe()).toBeDefined();
  });
});
